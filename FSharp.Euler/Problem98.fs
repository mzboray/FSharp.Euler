namespace FSharp.Euler.Problems

open System
open System.IO
open FSharp.Euler.Common.Combinatorics
open Checked

module Problem98 = 

//  let formCandidateAnagrams word =
//    let rec loop w =
//      match List.ofSeq w with
//      | [] -> []
//      | head :: tail -> 
//        let mutable list = List.empty
//        for item in (loop tail) do 
          
//    loop word
//
  let solve = ()
//    let words = File.ReadAllText("words_98.txt").Split(',') |> Array.map (fun s -> s.Trim('\"'))
//    for words in words
//      for c in word
//        